packages:
  all:
    providers:
      gl: [egl]
  egl:
    externals:
    - spec: egl@1.0.0
      prefix: /usr
    buildable: false
  llvm:
    require: llvm ~gold ~cuda

  # continuations branch available from here
  openmpi:
    package_attributes:
      git: https://github.com/biddisco/ompi

